# 模型1：特征转换ControlNet (FeatureControlNet)

## 📋 概述

**特征转换ControlNet** 是一个受 ControlNet 启发的模型，用于将姿势信息和特征信息转换为目标角度的特征，同时保持身份一致性。

### 核心功能

- **输入**：源特征 + 源姿势 + 控制角度
- **输出**：目标角度的特征（保持身份一致性）
- **应用场景**：从不同角度的特征生成目标角度的特征

---

## 🏗️ 模型架构

### 整体架构图

```
输入特征 [batch, 512] + 源姿势 [batch, 3]
    ↓
主网络（特征转换网络）
    ├─ 输入投影
    ├─ 角度位置编码
    ├─ Transformer层（3层）
    └─ 角度条件归一化
    ↓
主网络输出 [batch, 512]
    ↓
控制分支（接收控制角度）
    ├─ 角度编码器
    ├─ 控制信号生成层（3层）
    └─ 输出投影
    ↓
控制信号 [batch, 512]
    ↓
零卷积（Zero Convolution）
    ↓
控制输出 [batch, 512]
    ↓
融合：主网络输出 + 控制输出
    ↓
身份保护层
    ├─ 线性层 + LayerNorm
    ├─ ReLU
    └─ 线性层
    ↓
最终输出 [batch, 512]
```

---

## 🔧 核心组件

### 1. 主网络 (MainNetwork)

**功能**：将输入特征和姿势转换为目标角度的特征

**架构**：
```python
MainNetwork(
    feature_dim=512,      # 特征维度
    pose_dim=3,          # 姿势维度（欧拉角）
    hidden_dim=512,      # 隐藏层维度
    num_layers=3         # Transformer层数
)
```

**处理流程**：
1. **输入投影**：将 `[特征, 姿势]` 拼接并投影到隐藏维度
2. **角度位置编码**：为目标角度添加位置编码
3. **Transformer层**：3层MLP（带残差连接和LayerNorm）
4. **角度条件归一化**：根据目标角度进行条件归一化
5. **输出投影**：投影回特征维度

**特点**：
- ✅ 可以冻结（`freeze=True`），只训练控制分支
- ✅ 使用角度条件归一化，增强角度感知能力

### 2. 控制分支 (ControlBranch)

**功能**：接收控制角度，生成控制信号

**架构**：
```python
ControlBranch(
    control_angle_dim=3,  # 控制角度维度
    feature_dim=512,      # 特征维度
    hidden_dim=256,      # 隐藏层维度
    num_layers=3         # 层数
)
```

**处理流程**：
1. **角度编码器**：将控制角度编码为隐藏特征
2. **控制信号生成层**：3层MLP（带LayerNorm和Dropout）
3. **输出投影**：投影到特征维度

**特点**：
- ✅ 专门学习如何根据控制角度生成控制信号
- ✅ 独立于主网络，可以单独训练

### 3. 零卷积 (ZeroConv1d)

**功能**：ControlNet的核心创新，初始时输出为零

**原理**：
```python
ZeroConv1d(in_channels=512, out_channels=512)
# 初始权重和偏置都为零
```

**作用**：
- **初始阶段**：控制信号输出为零，不干扰主网络
- **训练过程**：逐渐学习如何控制主网络
- **最终效果**：精确控制生成过程，同时保持主网络能力

**优势**：
- ✅ 保护预训练的主网络能力
- ✅ 允许控制分支逐步学习
- ✅ 避免训练初期的不稳定性

### 4. 身份保护层 (Identity Protection)

**功能**：确保输出特征保持身份信息

**架构**：
```python
IdentityProtection(
    Linear(512, 512)
    LayerNorm(512)
    ReLU
    Linear(512, 512)
)
```

**融合策略**：
```python
identity_features = identity_protection(source_features)
output = 0.7 * fused_features + 0.3 * identity_features
```

**特点**：
- ✅ 从源特征中提取身份信息
- ✅ 与融合特征加权组合，保护身份一致性
- ✅ 权重可调（当前：0.7融合 + 0.3身份）

---

## 🔄 前向传播流程

### 详细步骤

```python
def forward(features, pose, control_angle):
    # ========== 1. 主网络处理 ==========
    main_output = main_network(features, pose, control_angle)
    # [batch, 512]
    
    # ========== 2. 控制分支生成控制信号 ==========
    control_signal = control_branch(control_angle)
    # [batch, 512]
    
    # ========== 3. 零卷积处理控制信号 ==========
    control_output = zero_conv(control_signal)
    # [batch, 512] (初始时为零)
    
    # ========== 4. 融合主网络输出和控制信号 ==========
    fused_features = main_output + control_output
    # [batch, 512]
    
    # ========== 5. 身份保护 ==========
    identity_features = identity_protection(features)
    # [batch, 512]
    
    # ========== 6. 最终输出 ==========
    output_features = 0.7 * fused_features + 0.3 * identity_features
    # [batch, 512]
    
    return output_features
```

---

## 📊 输入输出格式

### 输入

```python
features: torch.Tensor      # [batch, 512] 源特征
pose: torch.Tensor         # [batch, 3] 源姿势（欧拉角：yaw, pitch, roll）
control_angle: torch.Tensor # [batch, 3] 控制角度（目标角度）
```

### 输出

```python
output_features: torch.Tensor  # [batch, 512] 目标角度的特征
control_signal: torch.Tensor   # [batch, 512] 控制信号（可选）
```

---

## 🎯 训练目标

### 损失函数

模型使用两个损失函数：

1. **特征损失**（主要）：
   ```python
   feature_loss = MSE(output_features, target_features)
   ```

2. **身份一致性损失**（辅助）：
   ```python
   identity_loss = (1 - cosine_similarity(output_features, source_features)) * weight
   ```

3. **总损失**：
   ```python
   total_loss = feature_loss + identity_loss
   ```

### 训练策略

**策略1：冻结主网络**
```python
model = FeatureControlNet(freeze_main=True)
# 只训练控制分支和身份保护层
```

**策略2：端到端训练**
```python
model = FeatureControlNet(freeze_main=False)
# 训练所有参数
```

**推荐**：
- **第一阶段**：冻结主网络，快速训练控制分支
- **第二阶段**：解冻所有参数，端到端微调

---

## 🔍 关键特性

### 1. 角度感知

- **角度位置编码**：为目标角度添加位置编码
- **角度条件归一化**：根据目标角度进行条件归一化
- **角度编码器**：在控制分支中编码控制角度

### 2. 身份一致性保护

- **身份保护层**：从源特征提取身份信息
- **加权融合**：融合特征 + 身份特征
- **身份一致性损失**：在训练中约束身份信息

### 3. 精确控制

- **零卷积**：初始时不干扰主网络
- **控制分支**：专门学习控制信号
- **渐进式学习**：训练过程中逐渐学习控制

---

## 📈 模型参数

### 默认配置

```python
FeatureControlNet(
    feature_dim=512,           # 特征维度
    pose_dim=3,                # 姿势维度
    hidden_dim=512,            # 隐藏层维度
    num_main_layers=3,         # 主网络层数
    num_control_layers=3,      # 控制分支层数
    freeze_main=False          # 是否冻结主网络
)
```

### 参数量

- **总参数量**：约 2-3M（取决于配置）
- **可训练参数**：
  - 冻结主网络：约 0.5M（仅控制分支）
  - 端到端训练：约 2-3M（所有参数）

---

## 🚀 使用示例

### 基本使用

```python
from train_transformer3D.controlnetlike import FeatureControlNet
import torch

# 创建模型
model = FeatureControlNet(
    feature_dim=512,
    pose_dim=3,
    hidden_dim=512,
    num_main_layers=3,
    num_control_layers=3,
    freeze_main=False
)

# 准备输入
batch_size = 4
source_features = torch.randn(batch_size, 512)  # 源特征
source_pose = torch.randn(batch_size, 3)       # 源姿势
target_angle = torch.randn(batch_size, 3)     # 目标角度（控制角度）

# 前向传播
output_features, control_signal = model(
    features=source_features,
    pose=source_pose,
    control_angle=target_angle,
    return_control_signal=True
)

print(f"输出特征形状: {output_features.shape}")  # [4, 512]
print(f"控制信号形状: {control_signal.shape}")    # [4, 512]
```

### 训练示例

```python
# 计算损失
criterion = nn.MSELoss()
target_features = ...  # 目标特征（ground truth）

# 特征损失
feature_loss = criterion(output_features, target_features)

# 身份一致性损失
source_norm = F.normalize(source_features, p=2, dim=1)
output_norm = F.normalize(output_features, p=2, dim=1)
cosine_sim = (source_norm * output_norm).sum(dim=1).mean()
identity_loss = (1 - cosine_sim) * 0.3

# 总损失
total_loss = feature_loss + identity_loss

# 反向传播
total_loss.backward()
optimizer.step()
```

---

## ⚙️ 训练参数

### 推荐配置

```python
# 优化器
optimizer = optim.AdamW(
    model.parameters(),
    lr=1e-4,
    weight_decay=1e-5
)

# 学习率调度器
scheduler = optim.lr_scheduler.CosineAnnealingLR(
    optimizer,
    T_max=100,
    eta_min=1e-6
)

# 损失权重
identity_loss_weight = 0.3  # 身份一致性损失权重
```

### 训练技巧

1. **梯度裁剪**：
   ```python
   torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
   ```

2. **混合精度训练**：
   ```python
   with autocast():
       output = model(...)
   ```

3. **梯度累积**：
   ```python
   loss = loss / gradient_accumulation_steps
   ```

---

## 📊 评估指标

### 主要指标

1. **特征损失**：MSE损失
2. **余弦相似度**：输出特征与目标特征的相似度
3. **身份一致性**：输出特征与源特征的相似度

### TensorBoard可视化

- `Train/Loss`：训练总损失
- `Train/FeatureLoss`：特征损失
- `Train/IdentityLoss`：身份损失
- `Epoch/ValCosineSim`：验证余弦相似度
- `Features/SimilarityMatrix`：特征相似度矩阵（每10个epoch）

---

## 🔬 技术细节

### 角度提取

从关键点提取姿势（欧拉角）：
```python
def _extract_pose_from_keypoints(keypoints):
    # yaw: 使用左右眼x坐标差异
    yaw = arctan2(right_eye_x - left_eye_x, 1.0) * 180 / π
    
    # pitch: 使用鼻子和眼睛y坐标差异
    pitch = arctan2(nose_y - eye_y, 1.0) * 180 / π
    
    # roll: 使用左右眼连线角度
    roll = arctan2(right_eye_y - left_eye_y, right_eye_x - left_eye_x) * 180 / π
    
    return [yaw, pitch, roll]
```

### 角度条件归一化

```python
class AngleConditionedLayerNorm(nn.Module):
    """根据角度进行条件归一化"""
    def forward(self, x, angles):
        # 编码角度
        angle_features = encode_angles(angles)
        
        # 条件归一化
        mean = x.mean(dim=-1, keepdim=True)
        std = x.std(dim=-1, keepdim=True)
        normalized = (x - mean) / (std + eps)
        
        # 应用角度条件
        output = normalized * (1 + angle_features)
        
        return output
```

---

## ⚠️ 注意事项

### 1. 数据配对

- 确保源特征和目标特征来自**同一个人**
- 角度差异应在合理范围内（5° - 90°）

### 2. 训练策略

- **建议**：先冻结主网络训练控制分支，再端到端微调
- **学习率**：控制分支可以用稍高的学习率

### 3. 身份一致性

- 身份损失权重不宜过大（建议 0.3）
- 过大的身份损失可能阻碍角度转换

### 4. 零卷积初始化

- 零卷积确保初始时控制信号为零
- 训练初期主要依赖主网络，逐渐学习控制

---

## 📝 总结

**特征转换ControlNet** 是一个强大的模型，用于：

- ✅ **精确控制**：通过控制角度精确控制特征生成
- ✅ **身份一致性**：保持源特征的身份信息
- ✅ **渐进式学习**：零卷积确保训练稳定性
- ✅ **灵活训练**：支持冻结主网络或端到端训练

**适用场景**：
- 从侧面特征生成正面特征
- 从任意角度特征生成目标角度特征
- 保持身份一致性的特征转换

---

生成时间: 2024-12-16

